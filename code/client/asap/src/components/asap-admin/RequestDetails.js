import React from 'react';

const RequestDetails = ({ title }) => {
    return (
        <div>
            <label>{title}</label>
            <input type="text" />
        </div>
    );
};

export default RequestDetails;
